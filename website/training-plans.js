/* =====================================================================
 * 训练计划模块（周课表版）
 * 架构：经典脚本 + window.PlanModule 命名空间 + document 级事件委托
 * 数据：复用全局 store(localStorage)；训练记录写入全局 records
 * 依赖（app.js 提供）：store/$/$$/esc/toast/pad/todayStr/addDays/ring/
 *   beep/haptic/unlockAudio/acquireWake/releaseWake/askNotify/settings/
 *   records/saveRecords/genId/thumbFor
 * ===================================================================== */
'use strict';
(function () {
  /* ---------------- 周课表数据 ----------------
   * week: 周一→周日，长度 7；null=完全休息；{f:主题,e:[[动作,组数,次数,休息秒],...]}
   * 次数支持：'12' / '30秒' / '1分钟' / '力竭' / '15次' / '12（每侧）'
   */
  const PLANS = [
    {
      id: 'tp-mu1', title: '新手增肌 · 全身唤醒', goal: '增肌', level: '新手',
      desc: '每周三练全身力量，动作标准优先，帮助新手建立力量底座。',
      week: [
        { f: '全身力量 A', e: [['杠铃深蹲', 3, '12', 90], ['哑铃卧推', 3, '12', 90], ['坐姿划船', 3, '12', 90], ['哑铃推举', 3, '10', 75], ['平板支撑', 3, '30秒', 60]] },
        { f: '主动恢复', e: [['快走', 1, '20分钟', 0], ['腿部后侧拉伸', 2, '30秒', 30], ['猫式伸展', 2, '10次', 30]] },
        { f: '全身力量 B', e: [['弓步蹲', 3, '12', 90], ['俯卧撑', 3, '10', 90], ['引体向上', 3, '6', 90], ['臀桥', 3, '15', 60], ['卷腹', 3, '15', 60]] },
        null,
        { f: '全身力量 A', e: [['杠铃深蹲', 3, '12', 90], ['哑铃卧推', 3, '12', 90], ['坐姿划船', 3, '12', 90], ['哑铃推举', 3, '10', 75], ['平板支撑', 3, '30秒', 60]] },
        { f: '轻有氧 + 柔韧', e: [['跳绳', 3, '1分钟', 60], ['肩部环绕', 2, '10次', 30], ['婴儿式', 2, '30秒', 30], ['下犬式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-mu2', title: '进阶增肌 · 上下肢分化', goal: '增肌', level: '进阶',
      desc: '上下肢四练分化，容量与强度递进，适合有 3 个月训练基础者。',
      week: [
        { f: '上肢推', e: [['杠铃卧推', 4, '10', 120], ['哑铃推举', 4, '10', 90], ['双杠臂屈伸', 3, '12', 90], ['侧平举', 3, '15', 60], ['绳索下压', 3, '15', 60]] },
        { f: '下肢力量', e: [['杠铃深蹲', 4, '8', 150], ['罗马尼亚硬拉', 4, '10', 120], ['箭步蹲', 3, '12', 90], ['提踵', 4, '15', 60], ['平板支撑', 3, '45秒', 60]] },
        { f: '主动恢复', e: [['快走', 1, '25分钟', 0], ['腿部后侧拉伸', 3, '30秒', 30], ['猫式伸展', 2, '10次', 30]] },
        { f: '上肢拉', e: [['引体向上', 4, '8', 120], ['杠铃划船', 4, '10', 120], ['反向飞鸟', 3, '15', 60], ['哑铃弯举', 3, '12', 60], ['面拉', 3, '15', 60]] },
        { f: '下肢塑形', e: [['前蹲', 3, '10', 120], ['壶铃摆动', 3, '15', 90], ['单腿臀桥', 3, '12', 60], ['哥萨克深蹲', 3, '10', 60], ['死虫式', 3, '12', 60]] },
        { f: '轻有氧', e: [['慢跑', 1, '20分钟', 0], ['肩部环绕', 2, '10次', 30], ['下犬式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-mu3', title: '高手增肌 · 推拉腿', goal: '增肌', level: '高手',
      desc: '推拉腿五练高频分化，大重量复合动作为主，追求最大力量与肌肥大。',
      week: [
        { f: '推 · 胸肩三头', e: [['杠铃卧推', 5, '5', 180], ['上斜哑铃卧推', 4, '8', 120], ['站姿推举', 4, '8', 120], ['双杠臂屈伸', 3, '力竭', 90], ['绳索下压', 3, '12', 60]] },
        { f: '拉 · 背二头', e: [['硬拉', 4, '5', 180], ['引体向上', 4, '力竭', 120], ['杠铃划船', 4, '8', 120], ['高位下拉', 3, '12', 90], ['哑铃弯举', 3, '12', 60]] },
        { f: '腿 · 深蹲日', e: [['杠铃深蹲', 5, '5', 180], ['前蹲', 3, '8', 150], ['罗马尼亚硬拉', 3, '10', 120], ['箭步蹲', 3, '12', 90], ['提踵', 4, '15', 60]] },
        { f: '主动恢复', e: [['快走', 1, '30分钟', 0], ['泡沫轴放松', 2, '1分钟', 30], ['婴儿式', 2, '30秒', 30]] },
        { f: '推（容量日）', e: [['哑铃卧推', 4, '10', 120], ['哑铃推举', 3, '12', 90], ['侧平举', 4, '15', 60], ['窄距俯卧撑', 3, '力竭', 60], ['臂屈伸', 3, '12', 60]] },
        { f: '拉 + 核心', e: [['坐姿划船', 4, '10', 120], ['反向飞鸟', 4, '15', 60], ['哑铃弯举', 4, '10', 60], ['悬垂举腿', 3, '12', 60], ['平板支撑', 3, '45秒', 60]] },
        null
      ]
    },
    {
      id: 'tp-fa1', title: '新手减脂 · 低冲击燃脂', goal: '减脂', level: '新手',
      desc: '低冲击有氧 + 轻力量，保护膝踝，大体重与久坐人群友好。',
      week: [
        { f: '有氧入门', e: [['快走', 1, '30分钟', 0], ['靠墙静蹲', 3, '30秒', 45], ['开合跳', 3, '30秒', 45]] },
        { f: '拉伸恢复', e: [['腿部后侧拉伸', 2, '30秒', 30], ['肩部环绕', 2, '10次', 30], ['猫式伸展', 2, '10次', 30]] },
        { f: '轻间歇', e: [['跳绳', 5, '1分钟', 60], ['深蹲', 3, '15', 60], ['登山跑', 3, '30秒', 45], ['平板支撑', 3, '30秒', 45]] },
        { f: '主动恢复', e: [['散步', 1, '20分钟', 0], ['下犬式', 2, '30秒', 30]] },
        { f: '循环燃脂', e: [['高抬腿', 4, '30秒', 45], ['弓步蹲', 3, '12', 60], ['波比跳', 3, '8', 60], ['卷腹', 3, '15', 45]] },
        { f: '户外有氧', e: [['快走', 1, '40分钟', 0], ['腿部后侧拉伸', 2, '30秒', 30], ['婴儿式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-fa2', title: '进阶燃脂 · HIIT 强化', goal: '减脂', level: '进阶',
      desc: '高强度间歇 + 力量循环组合，每周四练，追求最大化燃脂效率。',
      week: [
        { f: 'HIIT A', e: [['波比跳', 4, '12', 45], ['深蹲跳', 4, '15', 45], ['登山跑', 4, '40秒', 30], ['开合跳', 4, '40秒', 30], ['平板支撑', 3, '45秒', 30]] },
        { f: '稳态有氧', e: [['慢跑', 1, '35分钟', 0], ['腿部后侧拉伸', 2, '30秒', 30], ['猫式伸展', 2, '10次', 30]] },
        { f: '力量循环', e: [['杠铃深蹲', 4, '10', 75], ['俯卧撑', 4, '12', 60], ['壶铃摆动', 4, '15', 60], ['臀桥', 4, '15', 45]] },
        null,
        { f: 'HIIT B', e: [['高抬腿', 4, '40秒', 30], ['弓步跳', 3, '16', 45], ['跳绳', 4, '1分钟', 45], ['俄罗斯转体', 3, '20', 30]] },
        { f: '全身循环', e: [['深蹲', 3, '15', 45], ['俯卧撑', 3, '12', 45], ['反向卷腹', 3, '15', 45], ['动态平板支撑', 3, '40秒', 45]] },
        null
      ]
    },
    {
      id: 'tp-sh1', title: '塑形 · 臀腿核心', goal: '塑形', level: '新手',
      desc: '臀腿 + 核心 + 上肢线条均衡雕刻，动作温和，女性居家友好。',
      week: [
        { f: '臀腿激活', e: [['臀桥', 4, '15', 60], ['蚌式开合', 3, '15', 45], ['螃蟹步', 3, '20步', 45], ['深蹲', 3, '15', 60], ['侧抬腿', 3, '15', 45]] },
        { f: '核心 + 拉伸', e: [['死虫式', 3, '12', 45], ['卷腹', 3, '15', 45], ['平板支撑', 3, '40秒', 45], ['婴儿式', 2, '30秒', 30]] },
        null,
        { f: '臀腿强化', e: [['单腿臀桥', 3, '12', 60], ['弓步蹲', 3, '12', 60], ['哥萨克深蹲', 3, '10', 60], ['提踵', 4, '20', 45]] },
        { f: '上肢线条', e: [['俯卧撑', 3, '10', 60], ['臂屈伸', 3, '12', 60], ['超人式', 3, '15', 45], ['门框天使', 3, '12', 45]] },
        { f: '轻有氧 + 柔韧', e: [['跳绳', 3, '1分钟', 60], ['下犬式', 2, '30秒', 30], ['腿部后侧拉伸', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-sh2', title: '进阶塑形 · 全身雕刻', goal: '塑形', level: '高手',
      desc: '力量爆发 + 高强度间歇，雕刻深层线条，适合训练老手。',
      week: [
        { f: '臀腿爆发', e: [['杠铃深蹲', 4, '10', 90], ['箱跳', 4, '8', 90], ['箭步蹲', 3, '12', 75], ['罗马尼亚硬拉', 3, '12', 75]] },
        { f: '上肢塑形', e: [['击掌俯卧撑', 4, '8', 90], ['引体向上', 4, '8', 90], ['哑铃推举', 3, '12', 75], ['钻石俯卧撑', 3, '力竭', 60]] },
        { f: '核心雕刻', e: [['悬垂举腿', 3, '12', 60], ['俄罗斯转体', 3, '20', 45], ['侧平板', 3, '30秒', 45], ['空中自行车', 3, '20', 45]] },
        { f: '主动恢复', e: [['散步', 1, '30分钟', 0], ['婴儿式', 2, '30秒', 30], ['猫式伸展', 2, '10次', 30]] },
        { f: '臀腿再练', e: [['壶铃摆动', 4, '15', 75], ['单腿深蹲', 3, '8', 75], ['单腿臀桥', 4, '12', 60], ['提踵', 4, '20', 45]] },
        { f: 'HIIT 收尾', e: [['波比跳', 4, '10', 45], ['高抬腿', 4, '40秒', 30], ['跳绳', 4, '1分钟', 45], ['平板支撑', 3, '45秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-re1', title: '康复训练 · 关节活动度', goal: '康复', level: '新手',
      desc: '颈肩、脊柱、髋膝分区康复，温和恢复活动度，久坐与术后恢复期适用。',
      week: [
        { f: '颈肩放松', e: [['颈部拉伸', 3, '30秒', 30], ['肩部环绕', 3, '10次', 30], ['门框天使', 3, '12', 30], ['毛巾颈后抗阻', 3, '10', 45]] },
        { f: '脊柱灵活', e: [['猫式伸展', 3, '10次', 30], ['眼镜蛇式', 3, '30秒', 30], ['婴儿式', 3, '30秒', 30], ['死虫式', 3, '10', 45]] },
        { f: '散步恢复', e: [['散步', 1, '25分钟', 0], ['肩部环绕', 2, '10次', 30]] },
        { f: '髋膝康复', e: [['臀桥', 3, '12', 45], ['座椅深蹲', 3, '12', 45], ['蚌式开合', 3, '12', 30], ['靠墙静蹲', 3, '30秒', 45]] },
        { f: '柔韧 + 盆底', e: [['下犬式', 3, '30秒', 30], ['腿部后侧拉伸', 3, '30秒', 30], ['凯格尔', 3, '10次', 30]] },
        { f: '户外散步', e: [['散步', 1, '30分钟', 0], ['猫式伸展', 2, '10次', 30]] },
        null
      ]
    },
    {
      id: 'tp-mu4', title: '居家增肌 · 哑铃力量', goal: '增肌', level: '新手',
      desc: '仅需一对哑铃，居家三练全身；动作简单易上手，在家也能稳步增肌。',
      week: [
        { f: '全身力量 A', e: [['哑铃深蹲', 3, '12', 90], ['哑铃卧推', 3, '12', 90], ['哑铃单臂划船', 3, '12', 90], ['站姿哑铃推举', 3, '10', 75], ['平板支撑', 3, '30秒', 60]] },
        { f: '主动恢复', e: [['快走', 1, '20分钟', 0], ['腿部后侧拉伸', 2, '30秒', 30], ['猫式伸展', 2, '10次', 30]] },
        { f: '全身力量 B', e: [['哑铃弓步', 3, '12', 90], ['哑铃飞鸟', 3, '12', 90], ['哑铃俯身划船', 3, '12', 90], ['哑铃侧平举', 3, '15', 60], ['哑铃臀桥', 3, '15', 60]] },
        null,
        { f: '全身力量 A', e: [['哑铃深蹲', 3, '12', 90], ['哑铃卧推', 3, '12', 90], ['哑铃单臂划船', 3, '12', 90], ['站姿哑铃推举', 3, '10', 75], ['平板支撑', 3, '30秒', 60]] },
        { f: '轻有氧 + 柔韧', e: [['跳绳', 3, '1分钟', 60], ['肩部环绕', 2, '10次', 30], ['下犬式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-pw1', title: '爆发增肌 · 运动表现', goal: '增肌', level: '高手',
      desc: '翻挺高拉 + 弹跳冲刺，提升力量速度与运动表现，适合追求爆发力的训练者。',
      week: [
        { f: '下肢爆发', e: [['高翻', 5, '3', 180], ['杠铃深蹲', 4, '5', 150], ['箱跳', 4, '6', 120], ['哑铃弓步', 3, '10', 90], ['站姿杠铃提踵', 3, '15', 60]] },
        { f: '上肢爆发', e: [['药球下砸', 4, '10', 90], ['击掌俯卧撑', 4, '8', 90], ['坐姿杠铃推举', 4, '6', 120], ['引体向上', 4, '6', 120], ['臂屈伸', 3, '力竭', 90]] },
        { f: '主动恢复', e: [['快走', 1, '30分钟', 0], ['泡沫轴放松', 2, '1分钟', 30], ['婴儿式', 2, '30秒', 30]] },
        { f: '全身力量', e: [['高拉', 5, '3', 180], ['杠铃硬拉', 4, '5', 180], ['杠铃卧推', 4, '6', 150], ['杠铃俯身划船', 4, '8', 120], ['悬垂举腿', 3, '12', 60]] },
        { f: '速度灵敏', e: [['30米冲刺', 6, '1', 90], ['跳箱', 4, '5', 90], ['绳梯脚步', 4, '30秒', 45], ['雪橇推', 3, '20米', 90], ['平板支撑', 3, '45秒', 60]] },
        { f: '轻有氧', e: [['慢跑', 1, '25分钟', 0], ['肩部环绕', 2, '10次', 30]] },
        null
      ]
    },
    {
      id: 'tp-fa3', title: '极限燃脂 · 混合体能', goal: '减脂', level: '高手',
      desc: '力量耐力 + 高强度间歇混合编排，每周五练，挑战体能极限、持续燃脂。',
      week: [
        { f: '代谢循环 A', e: [['壶铃摆动', 5, '20', 60], ['战绳', 4, '30秒', 45], ['哑铃火箭推', 4, '15', 60], ['波比跳', 4, '12', 45], ['登山跑', 4, '40秒', 30]] },
        { f: '冲刺间歇', e: [['400米冲刺', 6, '1', 120], ['平板支撑', 3, '45秒', 30]] },
        { f: '主动恢复', e: [['散步', 1, '30分钟', 0], ['泡沫轴放松', 2, '1分钟', 30], ['下犬式', 2, '30秒', 30]] },
        { f: '力量循环', e: [['杠铃深蹲', 4, '10', 90], ['杠铃硬拉', 4, '8', 90], ['俯卧撑', 4, '15', 60], ['杠铃俯身划船', 4, '12', 75], ['杠铃臀桥', 4, '15', 60]] },
        { f: '代谢循环 B', e: [['高脚杯深蹲', 4, '15', 60], ['滑雪机', 4, '30秒', 45], ['哑铃弓步', 4, '16', 60], ['高抬腿', 4, '40秒', 30], ['跳绳', 4, '2分钟', 60]] },
        { f: '长有氧', e: [['慢跑', 1, '50分钟', 0], ['腿部后侧拉伸', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-fa4', title: '居家燃脂 · 徒手循环', goal: '减脂', level: '新手',
      desc: '零器械徒手动作，居家三练循环 + 户外快走，新手与大体重也能轻松开练。',
      week: [
        { f: '徒手循环 A', e: [['深蹲', 4, '20', 60], ['俯卧撑', 4, '10', 60], ['扶墙高抬腿', 4, '30秒', 45], ['臀桥', 4, '20', 45]] },
        { f: '轻有氧', e: [['快走', 1, '30分钟', 0]] },
        { f: '核心循环', e: [['肘触膝卷腹', 4, '15', 45], ['平板支撑', 3, '40秒', 45], ['死虫式', 3, '12', 45], ['俄罗斯转体', 3, '20', 45]] },
        { f: '主动恢复', e: [['散步', 1, '20分钟', 0], ['猫式伸展', 2, '10次', 30]] },
        { f: '徒手循环 B', e: [['弓步蹲', 3, '12', 60], ['波比跳', 3, '8', 60], ['登山跑', 3, '30秒', 45], ['开合跳', 3, '40秒', 45], ['跪姿平板触肩', 3, '12', 45]] },
        { f: '户外有氧', e: [['快走', 1, '40分钟', 0], ['腿部后侧拉伸', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-sh3', title: '进阶塑形 · 曲线雕刻', goal: '塑形', level: '进阶',
      desc: '臀腿肩背分化塑形，力量动作雕刻紧致曲线，适合有基础想塑形者。',
      week: [
        { f: '臀腿力量', e: [['杠铃深蹲', 4, '12', 120], ['罗马尼亚硬拉', 4, '12', 120], ['保加利亚分腿蹲', 3, '12', 90], ['臀推', 4, '12', 90], ['站姿杠铃提踵', 4, '20', 60]] },
        { f: '上肢线条', e: [['哑铃卧推', 4, '12', 90], ['杠铃俯身划船', 4, '12', 90], ['站姿哑铃推举', 3, '12', 75], ['三头下压', 3, '15', 60], ['杠铃弯举', 3, '15', 60]] },
        { f: '核心 + 轻有氧', e: [['悬垂举腿', 3, '12', 45], ['侧平板', 3, '30秒', 45], ['跳绳', 3, '2分钟', 45]] },
        null,
        { f: '臀腿塑形', e: [['相扑硬拉', 4, '12', 90], ['壶铃摆动', 4, '15', 75], ['直腿硬拉', 3, '12', 90], ['蚌式开合', 3, '20', 45], ['单腿臀桥', 3, '12', 60]] },
        { f: 'HIIT 收尾', e: [['波比跳', 3, '10', 45], ['高抬腿', 3, '40秒', 30], ['登山跑', 3, '30秒', 30], ['深蹲跳', 3, '15', 45]] },
        null
      ]
    },
    {
      id: 'tp-co1', title: '核心强化 · 钢铁腰腹', goal: '塑形', level: '进阶',
      desc: '前链后链旋转核心分区训练，配合燃脂有氧，练出稳定有力的腰腹。',
      week: [
        { f: '前链核心', e: [['悬垂举腿', 4, '12', 60], ['健腹轮', 3, '10', 60], ['平板支撑', 3, '60秒', 45], ['死虫式', 3, '12', 45]] },
        { f: '核心 + 有氧', e: [['跪姿绳索卷腹', 4, '12', 45], ['绳索反向卷腹', 3, '12', 45], ['慢跑', 1, '30分钟', 0]] },
        { f: '旋转核心', e: [['俄罗斯转体', 4, '20', 45], ['绳索伐木', 3, '12（每侧）', 45], ['转体平板支撑', 3, '30秒', 45], ['侧平板', 3, '40秒', 45]] },
        { f: '主动恢复', e: [['散步', 1, '25分钟', 0], ['婴儿式', 2, '30秒', 30], ['猫式伸展', 2, '10次', 30]] },
        { f: '后链核心', e: [['山羊挺身', 4, '15', 60], ['杠铃臀桥', 4, '20', 60], ['超人式', 3, '15', 45], ['鸟狗式', 3, '12', 45]] },
        { f: '燃脂循环', e: [['波比跳', 3, '10', 45], ['高抬腿', 3, '40秒', 30], ['登山跑', 3, '40秒', 30], ['动态平板支撑', 3, '40秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-re2', title: '伤后回归 · 力量重建', goal: '康复', level: '进阶',
      desc: '固定器械与轻重量自由力量结合，循序渐进重建伤后肌力与动作信心。',
      week: [
        { f: '下肢重建', e: [['高脚杯深蹲', 3, '10', 90], ['腿举', 3, '12', 90], ['坐姿腿屈伸', 3, '12', 60], ['坐姿腿弯举', 3, '12', 60], ['站姿杠铃提踵', 3, '15', 45]] },
        { f: '上肢重建', e: [['器械卧推', 3, '10', 90], ['坐姿划船', 3, '12', 90], ['器械肩推', 3, '10', 90], ['弹力带面拉', 3, '15', 60], ['三头下压', 3, '12', 60]] },
        { f: '有氧恢复', e: [['椭圆机', 1, '25分钟', 0]] },
        { f: '核心稳定', e: [['死虫式', 3, '10', 45], ['鸟狗式', 3, '10', 45], ['侧平板', 3, '20秒', 45], ['平板支撑', 3, '30秒', 45]] },
        { f: '全身整合', e: [['高脚杯深蹲', 3, '10', 90], ['哑铃卧推', 3, '10', 90], ['杠铃俯身划船', 3, '10', 90], ['杠铃臀桥', 3, '12', 60]] },
        { f: '户外活动', e: [['快走', 1, '35分钟', 0], ['腿部后侧拉伸', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-bw1', title: '徒手自重 · 零基础入门', goal: '徒手', level: '新手',
      desc: '零器械建立动作模式，深蹲、跪姿俯卧撑、臀桥打地基，每周三练稳步进阶。',
      week: [
        { f: '全身基础 A', e: [['深蹲', 3, '15', 60], ['跪姿俯卧撑', 3, '8', 75], ['臀桥', 3, '15', 60], ['平板支撑', 3, '20秒', 60]] },
        { f: '散步恢复', e: [['散步', 1, '20分钟', 0], ['猫式伸展', 2, '10次', 30]] },
        { f: '全身基础 B', e: [['靠墙静蹲', 3, '30秒', 60], ['跪姿俯卧撑', 3, '8', 75], ['鸟狗式', 3, '10', 45], ['卷腹', 3, '12', 45]] },
        null,
        { f: '全身基础 A', e: [['深蹲', 3, '15', 60], ['跪姿俯卧撑', 3, '8', 75], ['臀桥', 3, '15', 60], ['平板支撑', 3, '20秒', 60]] },
        { f: '柔韧放松', e: [['下犬式', 2, '30秒', 30], ['腿部后侧拉伸', 2, '30秒', 30], ['婴儿式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-bw2', title: '徒手增肌 · 进阶自重', goal: '徒手', level: '进阶',
      desc: '标准俯卧撑、引体与单腿蹲渐进超负荷，无器械也能持续增肌，每周四练。',
      week: [
        { f: '推 + 腿', e: [['俯卧撑', 4, '12', 90], ['保加利亚分腿蹲', 4, '10', 90], ['钻石俯卧撑', 3, '8', 75], ['箭步蹲', 3, '12', 75], ['平板支撑', 3, '45秒', 60]] },
        { f: '拉 + 核心', e: [['引体向上', 4, '6', 120], ['澳式引体', 3, '10', 90], ['悬垂举腿', 3, '10', 60], ['侧平板', 3, '30秒', 45]] },
        { f: '主动恢复', e: [['快走', 1, '25分钟', 0], ['猫式伸展', 2, '10次', 30]] },
        { f: '推（容量日）', e: [['俯卧撑', 4, '15', 75], ['臂屈伸', 4, '12', 75], ['窄距俯卧撑', 3, '10', 60], ['派克俯卧撑', 3, '8', 75], ['侧平板转体', 3, '10', 45]] },
        { f: '腿部爆发', e: [['深蹲跳', 4, '10', 90], ['单腿臀桥', 4, '12', 60], ['靠墙静蹲', 3, '45秒', 60], ['提踵', 4, '20', 45]] },
        { f: '轻有氧', e: [['慢跑', 1, '20分钟', 0], ['下犬式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-bw3', title: '徒手塑形 · 线条雕刻', goal: '徒手', level: '进阶',
      desc: '臀腿、上肢、核心循环雕刻紧致线条，无需器械，居家每周四练。',
      week: [
        { f: '臀腿线条', e: [['深蹲', 4, '15', 60], ['蚌式开合', 3, '20', 45], ['单腿臀桥', 4, '12', 60], ['侧抬腿', 3, '15', 45]] },
        { f: '上肢紧致', e: [['俯卧撑', 4, '12', 75], ['臂屈伸', 3, '12', 60], ['超人式', 3, '15', 45], ['门框天使', 3, '12', 45]] },
        null,
        { f: '核心雕刻', e: [['死虫式', 3, '12', 45], ['俄罗斯转体', 3, '20', 45], ['平板支撑', 3, '45秒', 45], ['空中自行车', 3, '20', 45]] },
        { f: '全身循环', e: [['波比跳', 3, '10', 60], ['弓步蹲', 3, '12', 60], ['俯卧撑', 3, '10', 60], ['登山跑', 3, '30秒', 45]] },
        { f: '柔韧放松', e: [['下犬式', 2, '30秒', 30], ['腿部后侧拉伸', 2, '30秒', 30], ['婴儿式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-bw4', title: '徒手燃脂 · 28 天蜕变', goal: '徒手', level: '新手',
      desc: '低冲击循环 + 间歇有氧交替编排，居家徒手燃脂，大体重友好，四周见效。',
      week: [
        { f: '燃脂循环 A', e: [['开合跳', 4, '40秒', 45], ['深蹲', 3, '15', 60], ['高抬腿', 3, '30秒', 45], ['平板支撑', 3, '30秒', 45]] },
        { f: '轻有氧', e: [['快走', 1, '30分钟', 0]] },
        { f: '燃脂循环 B', e: [['波比跳', 3, '8', 60], ['登山跑', 3, '30秒', 45], ['弓步蹲', 3, '12', 60], ['卷腹', 3, '15', 45]] },
        { f: '主动恢复', e: [['散步', 1, '20分钟', 0], ['婴儿式', 2, '30秒', 30]] },
        { f: '燃脂循环 C', e: [['深蹲跳', 3, '10', 60], ['扶墙高抬腿', 3, '30秒', 45], ['俯卧撑', 3, '8', 60], ['俄罗斯转体', 3, '20', 45]] },
        { f: '户外有氧', e: [['快走', 1, '40分钟', 0], ['腿部后侧拉伸', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-bw5', title: '徒手核心 · 腹肌撕裂', goal: '徒手', level: '进阶',
      desc: '前链、侧链、旋转与后链分区轰炸腰腹，配合燃脂有氧，练出清晰腹肌线条。',
      week: [
        { f: '前链轰炸', e: [['卷腹', 4, '20', 45], ['悬垂举腿', 3, '10', 60], ['平板支撑', 3, '45秒', 45], ['空中自行车', 3, '20', 45]] },
        { f: '有氧辅助', e: [['慢跑', 1, '25分钟', 0], ['腿部后侧拉伸', 2, '30秒', 30]] },
        { f: '侧链 + 旋转', e: [['侧平板', 3, '30秒', 45], ['俄罗斯转体', 4, '20', 45], ['侧平板转体', 3, '10', 45], ['死虫式', 3, '12', 45]] },
        null,
        { f: '后链 + 稳定', e: [['超人式', 3, '15', 45], ['臀桥', 4, '15', 45], ['鸟狗式', 3, '12', 45], ['平板支撑', 3, '45秒', 45]] },
        { f: '燃脂收尾', e: [['登山跑', 4, '30秒', 45], ['高抬腿', 3, '30秒', 45], ['开合跳', 3, '40秒', 45]] },
        null
      ]
    },
    {
      id: 'tp-bw6', title: '徒手爆发 · 动物流', goal: '徒手', level: '高手',
      desc: '熊爬蟹行动物流 + 跳跃爆发训练，提升协调性、灵活性与全身爆发力。',
      week: [
        { f: '动物流 A', e: [['熊爬', 4, '20米', 60], ['蟹行', 4, '20米', 60], ['波比跳', 4, '12', 60], ['平板支撑', 3, '45秒', 45]] },
        { f: '爆发力', e: [['深蹲跳', 5, '10', 75], ['击掌俯卧撑', 4, '8', 90], ['弓步跳', 4, '12', 75], ['高抬腿', 4, '30秒', 45]] },
        { f: '主动恢复', e: [['快走', 1, '30分钟', 0], ['猫式伸展', 2, '10次', 30]] },
        { f: '动物流 B', e: [['熊爬', 4, '25米', 60], ['蟹行', 3, '25米', 60], ['登山跑', 4, '40秒', 45], ['死虫式', 3, '12', 45]] },
        { f: '全身整合', e: [['波比跳', 4, '12', 60], ['单腿深蹲', 3, '6', 90], ['俯卧撑', 4, '12', 75], ['悬垂举腿', 3, '10', 60]] },
        { f: '轻有氧', e: [['慢跑', 1, '25分钟', 0], ['下犬式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-cs1', title: '街健入门 · 引体向上专项', goal: '街健', level: '新手',
      desc: '从悬挂到肩胛引体再到离心引体，阶梯式解锁人生第一个标准引体向上。',
      week: [
        { f: '悬挂与肩胛', e: [['单杠悬挂', 4, '20秒', 60], ['肩胛引体', 4, '8', 60], ['澳式引体', 4, '8', 75], ['死虫式', 3, '12', 45]] },
        { f: '推基础', e: [['俯卧撑', 4, '10', 75], ['臂屈伸', 3, '10', 75], ['平板支撑', 3, '30秒', 45]] },
        null,
        { f: '离心引体', e: [['离心引体', 5, '5', 90], ['澳式引体', 4, '10', 75], ['单杠悬挂', 3, '20秒', 60], ['悬垂举腿', 3, '8', 60]] },
        { f: '腿 + 核心', e: [['深蹲', 4, '15', 60], ['弓步蹲', 3, '12', 60], ['卷腹', 3, '15', 45]] },
        { f: '主动恢复', e: [['散步', 1, '25分钟', 0], ['猫式伸展', 2, '10次', 30]] },
        null
      ]
    },
    {
      id: 'tp-cs2', title: '街健进阶 · 双力臂解锁', goal: '街健', level: '进阶',
      desc: '爆发力引体 + 双杠撑起 + 转化专项三管齐下，系统解锁双力臂。',
      week: [
        { f: '爆发引体', e: [['引体向上', 5, '5', 120], ['高位引体', 4, '5', 90], ['跳跃双力臂', 4, '3', 120], ['悬垂举腿', 3, '10', 60]] },
        { f: '双杠专项', e: [['双杠臂屈伸', 4, '10', 90], ['俯卧撑', 3, '15', 60], ['侧平板', 3, '30秒', 45]] },
        { f: '主动恢复', e: [['快走', 1, '25分钟', 0], ['猫式伸展', 2, '10次', 30]] },
        { f: '转化训练', e: [['高位引体', 5, '3', 120], ['跳跃双力臂', 4, '3', 120], ['澳式引体', 3, '12', 75], ['平板支撑', 3, '45秒', 45]] },
        { f: '腿 + 核心', e: [['保加利亚分腿蹲', 4, '10', 90], ['单杠悬挂', 4, '20秒', 60], ['俄罗斯转体', 3, '20', 45]] },
        { f: '轻有氧', e: [['慢跑', 1, '20分钟', 0], ['肩部环绕', 2, '10次', 30]] },
        null
      ]
    },
    {
      id: 'tp-cs3', title: '街健神技 · 前水平', goal: '街健', level: '高手',
      desc: '团身→单腿→分腿完整进阶链，配合拉力容量训练，逐步攻克前水平。',
      week: [
        { f: '拉力基础', e: [['引体向上', 5, '8', 120], ['高位引体', 4, '5', 120], ['澳式引体', 4, '10', 90], ['悬垂举腿', 4, '10', 60]] },
        { f: '前水平进阶', e: [['团身前水平', 5, '10秒', 90], ['单腿前水平', 4, '6秒', 90], ['澳式引体', 4, '8', 90], ['平板支撑', 3, '45秒', 60]] },
        null,
        { f: '拉力容量', e: [['引体向上', 4, '10', 90], ['离心引体', 4, '5', 120], ['肩胛引体', 3, '8', 75], ['死虫式', 3, '12', 45]] },
        { f: '分腿前水平', e: [['分腿前水平', 5, '6秒', 120], ['团身前水平引体', 4, '5', 90], ['悬垂举腿', 4, '12', 60], ['侧平板', 3, '30秒', 45]] },
        { f: '主动恢复', e: [['散步', 1, '30分钟', 0], ['婴儿式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-cs4', title: '街健神技 · 人体旗帜', goal: '街健', level: '高手',
      desc: '侧链力量 + 垂直推杠发力双线并进，从垂直旗帜渐进到水平人体旗帜。',
      week: [
        { f: '侧链力量', e: [['侧平板', 4, '40秒', 45], ['侧平板转体', 4, '10', 45], ['引体向上', 4, '6', 120], ['悬垂举腿', 4, '10', 60]] },
        { f: '推杠专项', e: [['双杠臂屈伸', 4, '12', 90], ['俯卧撑', 4, '15', 75], ['靠墙倒立', 4, '30秒', 90], ['门框天使', 3, '12', 45]] },
        null,
        { f: '旗帜进阶', e: [['垂直旗帜', 4, '8秒', 120], ['侧平板', 4, '40秒', 45], ['肩胛引体', 3, '8', 75], ['平板支撑', 3, '45秒', 60]] },
        { f: '拉 + 核心', e: [['引体向上', 4, '8', 120], ['澳式引体', 3, '12', 75], ['俄罗斯转体', 4, '20', 45]] },
        { f: '主动恢复', e: [['快走', 1, '30分钟', 0], ['肩部环绕', 2, '10次', 30]] },
        null
      ]
    },
    {
      id: 'tp-cs5', title: '街健神技 · 单臂引体', goal: '街健', level: '高手',
      desc: '毛巾辅助 + 弓箭手 + 离心单臂渐进组合，向自重训练皇冠动作发起冲击。',
      week: [
        { f: '拉力强化', e: [['引体向上', 5, '8', 120], ['毛巾辅助单臂引体', 4, '3（每侧）', 150], ['悬垂举腿', 4, '10', 60], ['平板支撑', 3, '45秒', 60]] },
        { f: '离心专项', e: [['单臂离心引体', 4, '3（每侧）', 150], ['引体向上', 4, '8', 120], ['澳式引体', 3, '12', 75]] },
        null,
        { f: '爆发拉力', e: [['高位引体', 5, '3', 150], ['弓箭手引体', 4, '4（每侧）', 120], ['单杠悬挂', 4, '30秒', 60], ['死虫式', 3, '12', 45]] },
        { f: '辅助容量', e: [['毛巾辅助单臂引体', 5, '2（每侧）', 150], ['双杠臂屈伸', 4, '12', 90], ['悬垂举腿', 4, '12', 60], ['侧平板', 3, '30秒', 45]] },
        { f: '主动恢复', e: [['散步', 1, '30分钟', 0], ['婴儿式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-cs6', title: '倒立进阶 · 自由倒立撑', goal: '街健', level: '高手',
      desc: '靠墙倒立打基础、离墙平衡练控制、离心倒立撑建力量，三阶段解锁自由倒立撑。',
      week: [
        { f: '倒立基础', e: [['靠墙倒立', 5, '30秒', 90], ['靠墙倒立撑', 4, '6', 120], ['派克俯卧撑', 4, '10', 90], ['平板支撑', 3, '45秒', 60]] },
        { f: '拉力平衡', e: [['引体向上', 4, '8', 120], ['澳式引体', 3, '12', 75], ['超人式', 3, '15', 45]] },
        { f: '主动恢复', e: [['快走', 1, '25分钟', 0], ['肩部环绕', 2, '10次', 30]] },
        { f: '离墙平衡', e: [['离墙倒立', 5, '15秒', 120], ['靠墙倒立撑', 4, '8', 120], ['派克俯卧撑', 3, '12', 90]] },
        { f: '倒立撑进阶', e: [['离心自由倒立撑', 4, '4', 150], ['派克俯卧撑', 4, '12', 90], ['双杠臂屈伸', 4, '12', 90], ['悬垂举腿', 3, '10', 60]] },
        { f: '柔韧恢复', e: [['肩部环绕', 2, '10次', 30], ['猫式伸展', 2, '10次', 30], ['婴儿式', 2, '30秒', 30]] },
        null
      ]
    },
    {
      id: 'tp-yg1', title: '瑜伽入门 · 晨间唤醒', goal: '瑜伽', level: '新手',
      desc: '15-20 分钟温和流瑜伽序列，唤醒身体、改善晨僵，零基础友好。',
      week: [
        { f: '晨间唤醒流', e: [['山式', 2, '30秒', 15], ['猫式伸展', 3, '10次', 20], ['下犬式', 3, '30秒', 30], ['站立前屈', 3, '30秒', 20], ['婴儿式', 2, '45秒', 20]] },
        { f: '髋部打开', e: [['蝴蝶式', 3, '45秒', 20], ['鸽子式', 3, '30秒（每侧）', 20], ['低弓步', 3, '30秒（每侧）', 20], ['仰卧扭转', 2, '30秒（每侧）', 20]] },
        null,
        { f: '脊柱灵活', e: [['猫式伸展', 3, '10次', 20], ['眼镜蛇式', 3, '20秒', 20], ['坐角式', 3, '30秒', 20], ['仰卧扭转', 2, '30秒（每侧）', 20], ['婴儿式', 2, '45秒', 20]] },
        { f: '晨间唤醒流', e: [['山式', 2, '30秒', 15], ['猫式伸展', 3, '10次', 20], ['下犬式', 3, '30秒', 30], ['站立前屈', 3, '30秒', 20], ['婴儿式', 2, '45秒', 20]] },
        { f: '深度放松', e: [['蝴蝶式', 2, '60秒', 20], ['腿部后侧拉伸', 2, '30秒（每侧）', 20], ['摊尸式', 1, '5分钟', 0]] },
        null
      ]
    },
    {
      id: 'tp-yg2', title: '瑜伽进阶 · 柔韧与力量', goal: '瑜伽', level: '进阶',
      desc: '战士序列 + 平衡体式 + 核心流，提升柔韧、力量与身体控制。',
      week: [
        { f: '战士力量流', e: [['战士一式', 3, '30秒（每侧）', 20], ['战士二式', 3, '30秒（每侧）', 20], ['三角式', 3, '30秒（每侧）', 20], ['下犬式', 3, '40秒', 30], ['平板支撑', 3, '45秒', 45]] },
        { f: '平衡专注', e: [['树式', 3, '30秒（每侧）', 20], ['战士三式', 3, '20秒（每侧）', 30], ['鹰式', 3, '20秒（每侧）', 20], ['山式', 2, '45秒', 15]] },
        { f: '髋部深开', e: [['鸽子式', 3, '45秒（每侧）', 20], ['蜥蜴式', 3, '40秒（每侧）', 20], ['坐角式', 3, '45秒', 20], ['快乐婴儿式', 2, '60秒', 20]] },
        null,
        { f: '核心流', e: [['船式', 4, '20秒', 30], ['侧板式', 3, '20秒（每侧）', 30], ['平板支撑', 3, '45秒', 45], ['眼镜蛇式', 3, '20秒', 20]] },
        { f: '修复放松', e: [['婴儿式', 2, '60秒', 20], ['仰卧扭转', 2, '45秒（每侧）', 20], ['摊尸式', 1, '8分钟', 0]] },
        null
      ]
    },
    {
      id: 'tp-yg3', title: '睡前瑜伽 · 深度放松', goal: '瑜伽', level: '新手',
      desc: '睡前 15 分钟舒缓序列，释放一天紧张，改善入睡质量。',
      week: [
        { f: '舒缓放松', e: [['婴儿式', 2, '60秒', 20], ['仰卧扭转', 2, '45秒（每侧）', 20], ['快乐婴儿式', 2, '60秒', 20], ['摊尸式', 1, '5分钟', 0]] },
        { f: '肩颈释放', e: [['猫式伸展', 3, '10次', 20], ['穿针式', 2, '40秒（每侧）', 20], ['颈部侧拉伸', 2, '30秒（每侧）', 15], ['婴儿式', 2, '60秒', 20]] },
        null,
        { f: '腰背松解', e: [['仰卧扭转', 3, '45秒（每侧）', 20], ['桥式', 3, '30秒', 30], ['腿部后侧拉伸', 2, '40秒（每侧）', 20], ['摊尸式', 1, '5分钟', 0]] },
        { f: '髋部安抚', e: [['蝴蝶式', 2, '60秒', 20], ['鸽子式', 2, '45秒（每侧）', 20], ['仰卧抱膝', 2, '45秒', 15], ['摊尸式', 1, '6分钟', 0]] },
        { f: '全身沉静', e: [['山式', 2, '45秒', 15], ['站立前屈', 2, '40秒', 20], ['婴儿式', 2, '60秒', 20], ['摊尸式', 1, '8分钟', 0]] },
        null
      ]
    },
    {
      id: 'tp-rn1', title: '跑步入门 · 5K 养成', goal: '跑步', level: '新手',
      desc: '跑走结合渐进，8 周从零基础到连续跑完 5 公里，配速不求快只求稳。',
      week: [
        { f: '跑走结合', e: [['快走热身', 1, '5分钟', 0], ['慢跑', 8, '1分钟', 90], ['放松拉伸', 2, '30秒', 20]] },
        null,
        { f: '力量辅助', e: [['深蹲', 3, '15', 60], ['单腿臀桥', 3, '12', 45], ['提踵', 3, '15', 45], ['平板支撑', 3, '30秒', 45]] },
        null,
        { f: '跑走进阶', e: [['快走热身', 1, '5分钟', 0], ['慢跑', 6, '2分钟', 90], ['放松拉伸', 2, '30秒', 20]] },
        { f: '轻松跑', e: [['慢跑', 1, '15分钟', 0], ['腿部后侧拉伸', 2, '30秒', 20]] },
        null
      ]
    },
    {
      id: 'tp-rn2', title: '跑步进阶 · 10K 突破', goal: '跑步', level: '进阶',
      desc: '节奏跑 + 间歇 + 周末长距离组合，系统提升耐力，10 公里稳定完赛。',
      week: [
        { f: '间歇训练', e: [['慢跑热身', 1, '10分钟', 0], ['快跑', 6, '400米', 120], ['放松慢跑', 1, '5分钟', 0]] },
        null,
        { f: '节奏跑', e: [['慢跑热身', 1, '8分钟', 0], ['节奏跑', 1, '20分钟', 0], ['放松慢跑', 1, '5分钟', 0]] },
        { f: '力量辅助', e: [['保加利亚分腿蹲', 3, '10', 75], ['单腿臀桥', 3, '12', 45], ['悬垂举腿', 3, '10', 60], ['侧平板', 3, '30秒', 45]] },
        null,
        { f: '周末长距离', e: [['慢跑', 1, '50分钟', 0], ['放松拉伸', 2, '40秒', 20]] },
        null
      ]
    },
    {
      id: 'tp-rn3', title: '半马备赛 · 12 周课表', goal: '跑步', level: '高手',
      desc: '经典半马课表浓缩周模板：间歇、节奏、LSD 科学配比，目标安全完赛 21K。',
      week: [
        { f: '间歇日', e: [['慢跑热身', 1, '12分钟', 0], ['快跑', 8, '400米', 90], ['放松慢跑', 1, '8分钟', 0]] },
        { f: '恢复跑', e: [['慢跑', 1, '30分钟', 0], ['放松拉伸', 2, '40秒', 20]] },
        { f: '节奏跑', e: [['慢跑热身', 1, '10分钟', 0], ['节奏跑', 1, '30分钟', 0], ['放松慢跑', 1, '5分钟', 0]] },
        null,
        { f: '力量与核心', e: [['深蹲跳', 4, '10', 75], ['单腿臀桥', 4, '12', 60], ['平板支撑', 3, '45秒', 45], ['悬垂举腿', 3, '12', 60]] },
        { f: 'LSD 长距离', e: [['慢跑', 1, '90分钟', 0], ['放松拉伸', 3, '40秒', 20]] },
        null
      ]
    },
    {
      id: 'tp-pp1', title: '产后恢复 · 温和重启', goal: '康复', level: '新手',
      desc: '面向产后妈妈的温和重建计划：盆底激活、核心唤醒、低冲击全身恢复。',
      week: [
        { f: '盆底与呼吸', e: [['凯格尔运动', 4, '10', 30], ['腹式呼吸', 3, '1分钟', 20], ['猫式伸展', 3, '10次', 20], ['婴儿式', 2, '45秒', 20]] },
        null,
        { f: '核心唤醒', e: [['死虫式', 3, '8', 45], ['臀桥', 3, '12', 45], ['鸟狗式', 3, '8', 45], ['侧卧抬腿', 3, '12', 30]] },
        null,
        { f: '温和全身', e: [['靠墙静蹲', 3, '20秒', 60], ['跪姿俯卧撑', 3, '6', 60], ['臀桥', 3, '12', 45], ['散步', 1, '15分钟', 0]] },
        { f: '舒缓放松', e: [['散步', 1, '20分钟', 0], ['仰卧扭转', 2, '30秒（每侧）', 20], ['婴儿式', 2, '60秒', 20]] },
        null
      ]
    },
    {
      id: 'tp-of1', title: '办公室微健身 · 碎片充电', goal: '徒手', level: '新手',
      desc: '久坐族专属：工位旁就能完成的 10 分钟微训练，缓解肩颈腰背疲劳。',
      week: [
        { f: '肩颈舒缓', e: [['肩部环绕', 3, '10次', 20], ['颈部侧拉伸', 2, '30秒（每侧）', 15], ['门框天使', 3, '12', 30], ['猫式伸展', 2, '10次', 20]] },
        { f: '工位激活', e: [['靠墙静蹲', 3, '30秒', 45], ['提踵', 3, '20', 30], ['臂屈伸(椅)', 3, '10', 45], ['站姿转体', 2, '15', 20]] },
        null,
        { f: '腰背放松', e: [['站姿体前屈', 3, '20秒', 20], ['鸟狗式', 3, '10', 30], ['臀桥', 3, '15', 30], ['婴儿式', 2, '45秒', 20]] },
        { f: '工位激活', e: [['靠墙静蹲', 3, '30秒', 45], ['提踵', 3, '20', 30], ['臂屈伸(椅)', 3, '10', 45], ['站姿转体', 2, '15', 20]] },
        { f: '周五充能', e: [['开合跳', 3, '30秒', 30], ['深蹲', 3, '15', 45], ['俯卧撑', 3, '8', 45], ['平板支撑', 2, '30秒', 30]] },
        null
      ]
    }
  ];

  const GOALS = ['全部', '增肌', '减脂', '塑形', '康复', '徒手', '街健', '瑜伽', '跑步'];
  const LEVELS = ['全部', '新手', '进阶', '高手'];
  const WEEKDAYS = ['周一', '周二', '周三', '周四', '周五', '周六', '周日'];
  const KCAL_RATE = { '新手': 6, '进阶': 8, '高手': 10 };

  /* ---------------- 工具与估算 ---------------- */
  const todayIdx = () => (new Date().getDay() + 6) % 7;            // 周一=0
  const mondayOf = d => addDays(d, -todayIdx());
  const getActiveId = () => store.get('tp_active', '');
  const findPlan = id => PLANS.find(p => p.id === id);

  // 单次组做功秒数
  function workSec(reps) {
    const s = String(reps);
    const n = parseFloat(s.replace(/[^0-9.]/g, '')) || 10;
    if (s.includes('分钟')) return n * 60;
    if (s.includes('秒')) return n;
    if (s.includes('力竭')) return 25;
    return n * 3; // 次/步
  }
  // 一天课程：预计分钟 + 千卡
  function dayEstimate(plan, day) {
    if (!day) return { min: 0, kcal: 0 };
    let sec = 0;
    day.e.forEach(([n, sets, reps, rest]) => {
      sec += sets * workSec(reps) + (sets - 1) * (rest || 0) + 20; // 20s 换动作
    });
    const min = Math.max(1, Math.round(sec / 60));
    return { min, kcal: Math.round(min * KCAL_RATE[plan.level]) };
  }
  function planSummary(plan) {
    const days = plan.week.filter(Boolean);
    const mins = days.map(d => dayEstimate(plan, d).min);
    const trainDays = days.length;
    const avgMin = Math.round(mins.reduce((a, b) => a + b, 0) / trainDays);
    return { trainDays, avgMin };
  }
  // 本周已完成天数（按本周内实际打卡日期计，兼容提前/补练其他天课程）
  function weekDoneCount(plan, monday) {
    const dates = new Set();
    for (let i = 0; i < 7; i++) {
      const date = addDays(monday, i);
      if (records.some(r => r.planId === plan.id && r.date === date)) dates.add(date);
    }
    return dates.size;
  }

  /* ---------------- 组件：今日训练卡片 ---------------- */
  // 徽标：简约线性 SVG 图标（颜色由 CSS 控制，深浅色自适应）
  const ttIcon = paths => `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${paths}</svg>`;
  const TT_ICONS = {
    plan: ttIcon('<rect x="6" y="4.5" width="12" height="16" rx="2.5"/><path d="M9.5 2.8h5v3.4h-5z"/><path d="M9.3 11h5.4M9.3 15h3.4"/>'),
    rest: ttIcon('<path d="M17.2 13.6A6.5 6.5 0 0 1 9.9 5.4a6.5 6.5 0 1 0 7.3 8.2Z"/>'),
    done: ttIcon('<circle cx="12" cy="12" r="8.4"/><path d="m8.4 12.3 2.5 2.5 4.7-5.2"/>'),
    play: ttIcon('<path d="M8 5.4v13.2L18.4 12Z"/>'),
    pause: ttIcon('<path d="M8 5.4v13.2M16 5.4v13.2"/>'),
    timer: ttIcon('<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 3"/>')
  };
  function todayCard() {
    const id = getActiveId();
    const plan = findPlan(id);
    const di = todayIdx();
    if (!plan) {
      return `<div class="tp-today tp-today-empty" data-tp="tp-open" data-id="${PLANS[0].id}">
        <div class="tt-left">
          <div class="tt-badge">${TT_ICONS.plan}</div>
          <div><b>选择你的训练计划</b><small>按目标与难度挑选，开启本周课表</small></div>
        </div>
        <span class="tt-cta">去选择 ›</span>
      </div>`;
    }
    const day = plan.week[di];
    const trainedToday = records.some(r => r.planId === plan.id && r.date === todayStr());
    if (!day) {
      return `<div class="tp-today tp-today-rest">
        <div class="tt-left">
          <div class="tt-badge">${trainedToday ? TT_ICONS.done : TT_ICONS.rest}</div>
          <div><b>${trainedToday ? '今日已打卡' : '今天是休息日'}</b><small>${esc(plan.title)} · 散步拉伸帮助恢复</small></div>
        </div>
        <span class="tt-cta" data-tp="tp-open" data-id="${plan.id}">看课表</span>
      </div>`;
    }
    const est = dayEstimate(plan, day);
    return `<div class="tp-today">
      <div class="tt-top">
        <span class="tt-day">${WEEKDAYS[di]} · ${day.f}</span>
        <span class="tt-plan">${esc(plan.title)}</span>
      </div>
      <div class="tt-meta">
          <span class="ttm"><b>${day.e.length}</b><i>个动作</i></span>
          <span class="ttm"><b>${est.min}</b><i>分钟</i></span>
          <span class="ttm"><b>${grp(est.kcal)}</b><i>千卡</i></span>
        </div>
      <button class="tt-start" data-tp="tp-start" data-id="${plan.id}">▶ 开始今日训练</button>
    </div>`;
  }

  /* ---------------- 视图：计划列表（计划 Tab） ---------------- */
  let filter = { goal: '全部', level: '全部' };

  function planCardHTML(plan) {
    const monday = mondayOf(todayStr());
    const done = weekDoneCount(plan, monday);
    const sum = planSummary(plan);
    const pct = Math.round(done / sum.trainDays * 100);
    const active = getActiveId() === plan.id;
    return `<div class="tp-card ${active ? 'active' : ''}" data-tp="tp-open" data-id="${plan.id}">
      <div class="tc-main">
        <div class="tc-tags">
          <span class="tp-pill g">${plan.goal}</span>
          <span class="tp-pill l">${plan.level}</span>
          ${active ? '<span class="tp-pill a">进行中</span>' : ''}
        </div>
        <b class="tc-title">${esc(plan.title)}</b>
        <small class="tc-sub">每周 ${sum.trainDays} 练 · 日均 ${sum.avgMin} 分钟 · 本周 ${done}/${sum.trainDays} 天</small>
      </div>
      <div class="tc-ring">
        ${ring(pct, 'var(--primary)', 'var(--divider)')}
        <span class="tc-ring-tx">${pct}%</span>
      </div>
    </div>`;
  }

  function filteredPlans() {
    return PLANS.filter(p =>
      (filter.goal === '全部' || p.goal === filter.goal) &&
      (filter.level === '全部' || p.level === filter.level));
  }
  function filtersInnerHTML() {
    return `<div class="chips">${GOALS.map(g => `<span class="chip ${filter.goal === g ? 'active' : ''}" data-tp="tp-filter" data-k="goal" data-v="${g}">${g}</span>`).join('')}</div>
      <div class="chips">${LEVELS.map(l => `<span class="chip ${filter.level === l ? 'active' : ''}" data-tp="tp-filter" data-k="level" data-v="${l}">${l}</span>`).join('')}</div>`;
  }
  function listWrapInnerHTML() {
    const list = filteredPlans();
    return list.length ? `<div class="tp-list">${list.map(planCardHTML).join('')}</div>`
      : emptyHTML('search', '该组合下暂无计划', '换个筛选试试');
  }
  function renderList() {
    appNoAnim();
    $('#app').innerHTML = `
      <div class="page-head"><h1>训练计划</h1><p>按目标与难度挑选 · 一周课表 · 组次与休息建议</p></div>
      <div class="tp-filters">${filtersInnerHTML()}</div>
      <div class="tp-list-wrap">${listWrapInnerHTML()}</div>
    `;
  }
  // 筛选：仅局部更新筛选条与列表，不重绘整页（避免白屏闪烁与滚动位置丢失）
  function refreshListInPlace() {
    const fEl = $('#app .tp-filters'), wEl = $('#app .tp-list-wrap');
    if (fEl) fEl.innerHTML = filtersInnerHTML();
    if (wEl) wEl.innerHTML = listWrapInnerHTML();
  }

  /* ---------------- 视图：计划详情（周课表） ---------------- */
  let detailSelDay = todayIdx();
  const footLabel = plan => {
    if (detailSelDay !== todayIdx()) return '预览第 ' + (detailSelDay + 1) + ' 天课程';
    return plan.week[detailSelDay] ? '▶ 开始今日训练' : '今天休息日 · 散步拉伸';
  };

  function renderDetail(id) {
    const plan = findPlan(id);
    if (!plan) return;
    const monday = mondayOf(todayStr());
    const done = weekDoneCount(plan, monday);
    const sum = planSummary(plan);
    const pct = Math.round(done / sum.trainDays * 100);
    const active = getActiveId() === plan.id;
    const sub = document.createElement('div');
    sub.className = 'subpage tp-detail-page';
    sub.dataset.ctx = 'detail';
    sub.innerHTML = `
      <div class="sp-head">
        <button class="icon-btn" data-tp="tp-back" aria-label="返回">‹</button>
        <b>${esc(plan.title)}</b>
        <button class="tp-set-btn ${active ? 'on' : ''}" data-tp="tp-set" data-id="${plan.id}">${active ? '✓ 我的计划' : '设为我的计划'}</button>
      </div>
      <div class="sp-body">
        <div class="tp-d-head">
          <div class="tc-ring lg">${ring(pct, 'var(--primary)', 'var(--divider)')}<span class="tc-ring-tx">${pct}%</span></div>
          <div class="tp-d-info">
            <div class="tc-tags"><span class="tp-pill g">${plan.goal}</span><span class="tp-pill l">${plan.level}</span></div>
            <p class="muted" style="margin-top:8px;line-height:1.6">${esc(plan.desc)}</p>
            <p class="muted">每周 ${sum.trainDays} 练 · 日均 ${sum.avgMin} 分钟 · 本周已完成 ${done} 天</p>
          </div>
        </div>
        <div class="tp-week">
          ${plan.week.map((day, i) => {
            const date = addDays(monday, i);
            const isToday = i === todayIdx();
            const isDone = day && records.some(r => r.planId === plan.id && r.date === date);
            const est = day ? dayEstimate(plan, day) : null;
            return `<div class="tp-day ${isToday ? 'today' : ''} ${day ? '' : 'rest'} ${i === detailSelDay ? 'sel' : ''}" data-tp="tp-day" data-d="${i}">
              <div class="td-head">
                <span class="td-wd">${WEEKDAYS[i]}</span>
                <span class="td-date">${date.slice(5).replace('-', '/')}${isToday ? ' · 今天' : ''}</span>
                ${isDone ? '<span class="td-done">✓ 已打卡</span>' : ''}
              </div>
              <div class="td-focus">${day ? `${day.f} <em>· ${est.min}分钟 · ${est.kcal}千卡</em>` : '<em>完全休息 · 散步拉伸即可</em>'}</div>
              ${day ? `<ul class="td-items">${day.e.map(([n, sets, reps, rest]) => `
                <li><span class="tdi-n">${esc(n)}</span><span class="tdi-v">${sets} × ${esc(reps)}</span><span class="tdi-r">${rest ? '休' + rest + '秒' : '—'}</span></li>`).join('')}</ul>` : ''}
            </div>`;
          }).join('')}
        </div>
      </div>
      <div class="tp-detail-foot">
        <button class="btn mega" data-tp="tp-detail-start" data-id="${plan.id}">
          ${footLabel(plan)}
        </button>
      </div>`;
    document.body.appendChild(sub);
    requestAnimationFrame(() => sub.classList.add('show'));
  }

  /* ---------------- 视图：训练执行 ---------------- */
  let sess = null, sessEl = null, sessIv = null;
  const fmtS = s => { s = Math.max(0, s | 0); return pad(Math.floor(s / 60)) + ':' + pad(s % 60); };
  const elapsed = () => sess ? sess.elapsed + (sess.running ? Date.now() - sess.runStart : 0) : 0;
  const persistSess = () => sess && store.set('tp_session', sess);
  const nextUndone = () => {
    for (let i = 0; i < sess.items.length; i++) if (!sess.done.includes(i)) return i;
    return null;
  };
  /* 计时类动作：从次数描述解析秒数（'30秒'→30，'1分钟'→60，'1.5分钟'→90）；非计时类返回 0 */
  const parseSecs = reps => {
    const m = /(\d+(?:\.\d+)?)\s*分钟/.exec(reps); if (m) return Math.round(m[1] * 60);
    const s = /(\d+(?:\.\d+)?)\s*秒/.exec(reps); if (s) return Math.round(s[1]);
    return 0;
  };

  function openSession(planId, dayIdxArg, saved) {
    const plan = findPlan(planId);
    const dayI = dayIdxArg === undefined ? todayIdx() : dayIdxArg;
    const day = plan && plan.week[dayI];
    if (!day) { toast('今天是休息日'); return; }
    const est = dayEstimate(plan, day);
    sess = saved || {
      planId, day: dayI, date: todayStr(), done: [],
      running: false, runStart: 0, elapsed: 0,
      restEnd: 0, restLen: 0, beeped: {},
      actTimerEnd: 0, actTimerBeeped: true
    };
    sess.items = day.e;
    const sub = document.createElement('div');
    sub.className = 'subpage tp-sess-page';
    sub.dataset.ctx = 'session';
    sub.innerHTML = `
      <div class="sp-head">
        <button class="icon-btn" data-tp="tp-sess-back" aria-label="返回（保留进度）">‹</button>
        <b>${esc(plan.title)}</b>
        <button class="tp-quit" data-tp="tp-quit" aria-label="放弃训练">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M6 6l12 12M18 6L6 18"/></svg>放弃
        </button>
      </div>
      <div class="sp-body tp-sess-body">
        <div class="tps-day">
          <span class="tp-pill g">${WEEKDAYS[dayI]} · ${day.f}</span>
          <span class="muted">预计 ${est.min} 分钟 · ${est.kcal} 千卡</span>
        </div>
        <div class="card tps-timer-card">
          <div class="tc-label">训练计时</div>
          <div class="tps-time" id="tpsTime">00:00</div>
          <button class="btn ghost tps-toggle" id="tpsToggle" data-tp="tp-timer">${TT_ICONS.play}<span>开始计时</span></button>
        </div>
        <div class="tps-rest" id="tpsRest" hidden>
          <div class="tr-label">😮‍💨 组间休息</div>
          <div class="tr-ring">
            <svg viewBox="0 0 120 120" width="120" height="120">
              <circle cx="60" cy="60" r="52" fill="none" stroke="var(--divider)" stroke-width="8"/>
              <circle id="trArc" cx="60" cy="60" r="52" fill="none" stroke="var(--warning)" stroke-width="8"
                stroke-linecap="round" transform="rotate(-90 60 60)"/>
            </svg>
            <div class="tr-num" id="trNum">00</div>
          </div>
          <button class="btn ghost full" data-tp="tp-skiprest">跳过休息，继续练</button>
        </div>
        <div class="card">
          <h3>动作清单 <span class="muted" id="tpsCount">0/${day.e.length}</span></h3>
          <div class="progress-mini"><i id="tpsBar" style="width:0%"></i></div>
          <div class="tps-list">
            ${day.e.map((it, i) => {
              const [n, sets, reps, rest] = it;
              const thumb = typeof thumbFor === 'function' ? thumbFor(n) : '';
              const tsecs = parseSecs(reps);
              return `<button type="button" class="tps-row ${sess.done.includes(i) ? 'done' : ''}" data-tp="tp-row" data-i="${i}">
                ${thumb ? `<span class="ex-thumb"><img loading="lazy" src="${thumb}" alt=""></span>` : '<span class="tps-noimg"></span>'}
                <span class="tps-n">${esc(n)}</span>
                <span class="tps-v">${sets} × ${esc(reps)}<em>·休${rest || 0}″</em></span>
                ${tsecs ? `<span class="tps-timer" data-tp="tp-act-timer" data-i="${i}" data-secs="${tsecs}" role="button" aria-label="${esc(n)} 计时 ${tsecs} 秒" title="点我开始 ${tsecs} 秒计时，时间到震动提醒">${TT_ICONS.timer}</span>` : ''}
                <span class="tps-demo" data-demo="${esc(n)}" role="button" aria-label="观看 ${n} 演示视频" title="看演示视频">▶</span>
                <span class="tps-state" aria-hidden="true">
                  <svg viewBox="0 0 24 24" width="15" height="15"><path d="M5 12.5l4.2 4.2L19 7" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
                </span>
              </button>`;
            }).join('')}
          </div>
        </div>
      </div>
      <div class="wk-foot tps-foot">
        <div class="wk-next" id="tpsNext"></div>
        <button class="btn mega" data-tp="tp-mega">✓ 完成第 1 个动作</button>
      </div>
      <div class="tp-celebrate" id="tpsCelebrate" hidden>
        <div class="tpc-pop">🎉</div>
        <h2>今日训练完成！</h2>
        <p class="muted" id="tpcStat"></p>
        <div class="tpc-confetti" id="tpcConfetti"></div>
        <div class="tpc-btns">
          <button class="btn mega" data-tp="tp-save">保存并打卡</button>
          <button class="btn ghost" data-tp="tp-continue">再练一会儿</button>
        </div>
      </div>`;
    document.body.appendChild(sub);
    requestAnimationFrame(() => sub.classList.add('show'));
    sessEl = sub;
    clearInterval(sessIv);
    sessIv = setInterval(renderTick, 250);
    renderTick(); updateSess(); persistSess();
    if (saved && (saved.elapsed > 0 || saved.done.length)) toast('已恢复上次训练 💪');
    if (sess.running) $('#tpsToggle').innerHTML = TT_ICONS.pause + '<span>暂停</span>';
  }

  function renderTick() {
    if (!sess || !sessEl) return;
    $('#tpsTime').textContent = fmtS(elapsed() / 1000);
    const restEl = $('#tpsRest');
    if (sess.restEnd) {
      const left = Math.ceil((sess.restEnd - Date.now()) / 1000);
      if (left > 0) {
        restEl.hidden = false;
        $('#trNum').textContent = pad(left);
        const r = 52, c = 2 * Math.PI * r, ratio = Math.max(0, left / Math.max(sess.restLen, 1));
        $('#trArc').setAttribute('stroke-dasharray', c);
        $('#trArc').setAttribute('stroke-dashoffset', c * (1 - ratio));
        if (left <= 3 && !sess.beeped[left]) { sess.beeped[left] = true; beep(880, .12); haptic(150); }
      } else {
        restEl.hidden = true; sess.restEnd = 0;
        beep(1175, .55, 'sine', .4); haptic([300, 100, 300]);
      }
    } else restEl.hidden = true;
    // 动作计时器：行内倒计时显示，时间到震动+声音提醒
    if (sess.actTimerEnd) {
      const left = Math.ceil((sess.actTimerEnd - Date.now()) / 1000);
      const tEl = sessEl.querySelector(`.tps-timer[data-i="${sess.actTimerI}"]`);
      if (left > 0) {
        if (tEl && tEl.textContent !== left + '″') tEl.textContent = left + '″';
        if (left <= 3 && !sess.actTimerBeeped) {
          sess.actTimerBeeped = true;
          beep(880, .12); haptic(150);
        }
      } else {
        if (tEl) { tEl.innerHTML = TT_ICONS.timer; tEl.classList.remove('run'); }
        sess.actTimerEnd = 0; sess.actTimerBeeped = true;
        beep(1175, .55, 'sine', .4); haptic([300, 100, 300]);
        toast('⏱ 时间到！' + sess.items[sess.actTimerI][0] + ' 可以停了');
      }
    }
    persistSess();
  }

  function completeOne(i) {
    if (!sess.done.includes(i)) sess.done.push(i);
    const row = sessEl.querySelector(`.tps-row[data-i="${i}"]`);
    if (row) row.classList.add('done');
    beep(660, .12); haptic(60);
    const rest = sess.items[i][3] || 0;
    // 最后一个动作不再启动休息
    if (rest && nextUndone() !== null) {
      sess.restLen = rest;
      sess.restEnd = Date.now() + rest * 1000;
      sess.beeped = {};
    } else sess.restEnd = 0;
    updateSess(); persistSess();
    if (nextUndone() === null) setTimeout(showCelebrate, 500);
  }

  function updateSess() {
    if (!sessEl) return;
    const n = sess.items.length;
    $('#tpsCount').textContent = sess.done.length + '/' + n;
    $('#tpsBar').style.width = (sess.done.length / n * 100) + '%';
    const next = nextUndone();
    const mega = sessEl.querySelector('.tps-foot .mega');
    const nextEl = $('#tpsNext');
    if (next === null) {
      mega.textContent = '🎉 查看完成报告';
      nextEl.textContent = '全部 ' + n + ' 个动作已完成';
    } else {
      mega.textContent = '✓ 完成第 ' + (next + 1) + ' 个动作';
      nextEl.textContent = '下一个 · ' + sess.items[next][0];
    }
    sessEl.querySelectorAll('.tps-row').forEach(r => {
      r.classList.toggle('done', sess.done.includes(+r.dataset.i));
    });
  }

  /* ---- 完成动画 ---- */
  function showCelebrate() {
    const el = $('#tpsCelebrate');
    if (!el || !sess) return;
    const plan = findPlan(sess.planId);
    const day = plan.week[sess.day];
    const est = dayEstimate(plan, day);
    const ratio = sess.done.length / sess.items.length;
    const min = Math.max(1, Math.round(Math.max(elapsed() / 60000, est.min * ratio)));
    const kcal = Math.round(est.kcal * ratio);
    $('#tpcStat').textContent = min + ' 分钟 · ' + kcal + ' 千卡 · ' + sess.done.length + '/' + sess.items.length + ' 个动作';
    el._min = min; el._kcal = kcal;
    // 生成彩带
    const box = $('#tpcConfetti');
    box.innerHTML = '';
    const colors = ['#00b578', '#ff7d00', '#36c99a', '#165dff', '#ff9a2e'];
    for (let i = 0; i < 24; i++) {
      const s = document.createElement('span');
      s.style.left = Math.random() * 100 + '%';
      s.style.background = colors[i % colors.length];
      s.style.animationDelay = (Math.random() * .5) + 's';
      s.style.animationDuration = (1.4 + Math.random() * 1.2) + 's';
      box.appendChild(s);
    }
    el.hidden = false;
    requestAnimationFrame(() => el.classList.add('on'));
  }

  function saveRecord() {
    const el = $('#tpsCelebrate');
    const plan = findPlan(sess.planId);
    records.unshift({
      id: genId(), planId: sess.planId, planTitle: plan.title,
      date: sess.date, minutes: el._min, kcal: el._kcal,
      doneCount: sess.done.length, total: sess.items.length
    });
    saveRecords();
    closeSession();
    store.set('tp_session', null);
    toast('打卡成功 · ' + el._min + ' 分钟 · ' + el._kcal + ' 千卡 🔥');
    showTab('stats');
  }

  function closeSession() {
    clearInterval(sessIv); sessIv = null;
    releaseWake();
    if (sessEl) {
      const el = sessEl;
      el.classList.remove('show');
      setTimeout(() => el.remove(), 220);
      sessEl = null;
    }
    sess = null;
  }

  /* ---------------- 事件委托（document 级，只绑一次） ---------------- */
  document.addEventListener('click', (e) => {
    const btn = e.target.closest('[data-tp]');
    if (!btn) return;
    const act = btn.dataset.tp;
    switch (act) {
      case 'tp-filter':
        filter[btn.dataset.k] = btn.dataset.v;
        if ($('#app') && $('#app').querySelector('.tp-filters')) refreshListInPlace();
        break;
      case 'tp-open': renderDetail(btn.dataset.id); break;
      case 'tp-set': {
        const id = btn.dataset.id;
        store.set('tp_active', getActiveId() === id ? '' : id);
        toast(getActiveId() === id ? '已设为我的计划' : '已取消计划');
        const oldPage = document.querySelector('.tp-detail-page.show');
        if (oldPage) closeSubpageEl(oldPage);
        if (getActiveId() === id) renderDetail(id);
        if ($('#app').querySelector('.tp-list')) renderList();
        break;
      }
      case 'tp-day':
        detailSelDay = +btn.dataset.d;
        const page = document.querySelector('.tp-detail-page.show');
        if (page) {
          page.querySelectorAll('.tp-day').forEach(d => d.classList.toggle('sel', +d.dataset.d === detailSelDay));
          const foot = page.querySelector('.tp-detail-foot .mega');
          foot.textContent = footLabel(findPlan(foot.dataset.id));
        }
        break;
      case 'tp-detail-start': {
        const plan = findPlan(btn.dataset.id);
        const day = plan.week[detailSelDay];
        if (!day) { toast('该天为休息日'); break; }
        closeSubpageEl(document.querySelector('.tp-detail-page.show'));
        openSession(btn.dataset.id, detailSelDay);
        break;
      }
      case 'tp-start':
        openSession(btn.dataset.id, todayIdx());
        break;
      case 'tp-back':
        closeSubpageEl(document.querySelector('.tp-detail-page.show'));
        break;
      case 'tp-sess-back':
        persistSess(); closeSession(); toast('进度已保留，稍后可恢复');
        break;
      case 'tp-quit':
        showConfirm({ title: '放弃本次训练？', desc: '当前进度将被清除，且不会计入训练记录', okText: '放弃训练', danger: true }).then(ok => {
          if (ok) { closeSession(); store.set('tp_session', null); toast('训练已放弃'); }
        });
        break;
      case 'tp-timer': {
        unlockAudio(); askNotify();
        const b = $('#tpsToggle');
        if (sess.running) {
          sess.elapsed += Date.now() - sess.runStart;
          sess.running = false; b.innerHTML = TT_ICONS.play + '<span>继续计时</span>'; releaseWake();
        } else {
          sess.running = true; sess.runStart = Date.now();
          b.innerHTML = TT_ICONS.pause + '<span>暂停</span>'; acquireWake();
        }
        persistSess(); renderTick();
        break;
      }
      case 'tp-row': {
        const i = +btn.dataset.i;
        if (sess.done.includes(i)) {
          sess.done = sess.done.filter(x => x !== i);
        } else {
          unlockAudio(); askNotify();
          completeOne(i);
        }
        updateSess(); persistSess();
        break;
      }
      case 'tp-act-timer': {
        e.stopPropagation(); // 防止触发行点击（完成/取消完成）
        unlockAudio(); askNotify();
        const i = +btn.dataset.i;
        const secs = +btn.dataset.secs;
        if (sess.actTimerEnd && sess.actTimerI === i) {
          // 再点一次取消
          sess.actTimerEnd = 0; sess.actTimerBeeped = true;
          btn.innerHTML = TT_ICONS.timer; btn.classList.remove('run');
          toast('已取消计时');
        } else {
          sess.actTimerI = i;
          sess.actTimerEnd = Date.now() + secs * 1000;
          sess.actTimerBeeped = false;
          btn.textContent = secs + '″'; btn.classList.add('run');
          toast('⏱ ' + sess.items[i][0] + ' 计时 ' + secs + ' 秒');
        }
        persistSess();
        break;
      }
      case 'tp-skiprest':
        sess.restEnd = 0; persistSess(); renderTick();
        break;
      case 'tp-mega': {
        const next = nextUndone();
        if (next === null) { showCelebrate(); break; }
        unlockAudio(); askNotify();
        if (!sess.running) {
          sess.running = true; sess.runStart = Date.now();
          $('#tpsToggle').innerHTML = TT_ICONS.pause + '<span>暂停</span>'; acquireWake();
        }
        completeOne(next);
        break;
      }
      case 'tp-continue': {
        const el = $('#tpsCelebrate');
        el.classList.remove('on'); el.hidden = true;
        break;
      }
      case 'tp-save': saveRecord(); break;
    }
  });

  function closeSubpageEl(el) {
    if (!el) return;
    el.classList.remove('show');
    setTimeout(() => el.remove(), 220);
  }

  /* ---------------- 恢复未完成会话 ---------------- */
  function resumeIfAny() {
    const s = store.get('tp_session', null);
    if (!s) return;
    if (s.date === todayStr() && (s.elapsed > 0 || (s.done && s.done.length) || s.running)) {
      openSession(s.planId, s.day, s);
    } else store.set('tp_session', null);
  }
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', resumeIfAny);
  } else resumeIfAny();

  /* ---------------- 对外 API ---------------- */
  window.PlanModule = {
    renderList, todayCard, resumeIfAny, planCardHTML,
    plans: PLANS, findPlan, dayEstimate, todayIdx
  };
})();
